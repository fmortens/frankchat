Very limited chat application

This app uses Firestore as a backend and local storage

To set up the backend you first need to create an application with firebase

The config data is then added to this file

You need to set up the following ...